#include <bits/stdc++.h>
using namespace std;
int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	int a, b;
	while (scanf("%d %d", &a, &b) != EOF)
	{
		cout << a+b << '\n';
	}
	
	return 0;
}