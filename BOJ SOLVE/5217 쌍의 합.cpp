#include <bits/stdc++.h>

using namespace std;

int a;

int main()
{
	int n;
	scanf("%d", &n);
	for (int i = 0; i < n; i++)
	{
		scanf("%d", &a);
		for (int j = 1; j < a; j++)
		{
			for (int k = 1; k < a; k++)
			{
				if (j != k)
				{
					if ((j + k) == a)
				}
				else
				{
					break;
				}
			}
		}
	}

	return 0;
}